# API base. Default is same-origin /v1:
#  - dev:  Vite proxies /v1 -> http://localhost:5057 (KdbVault.Api)
#  - prod: Caddy serves the SPA and reverse-proxies /v1 on the same origin
# Only set this if the API lives on another origin (cookies then need SameSite=None).
# VITE_API_URL=/v1
